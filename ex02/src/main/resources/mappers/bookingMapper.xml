<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="org.zerock.mapper.BookingMapper">
  <insert id="input_reviews">
  insert into tbl_htlreview(htl_name, htl_addr, writer, score, title, review_neg, review_pos, write_date)
  values(#{htl_name},#{htl_addr},#{writer},#{score},#{title},#{review_neg},#{review_pos},#{write_date})
  </insert>
  
  
  
  </mapper>
  
  